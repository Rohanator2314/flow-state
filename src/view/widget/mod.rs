//! Custom widgets. Currently a vendored, extended fork of iced's
//! `text_editor` (see [`text_editor`]) — flow-state needs character
//! underlines, paragraph glow, and typewriter-centering scroll control that
//! the stock widget does not expose.

pub mod text_editor;
